{
  "name": "xai-search-mcp",
  "version": "1.0.0",
  "description": "Streamable HTTP MCP Server for XAI X_Search - Deep search x.com posts via xAI Grok API with XAI_API_KEY authentication",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.0.95",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "wrangler": "^4.50.0"
  }
}
